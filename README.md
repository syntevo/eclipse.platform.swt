# About

This repository just implements native controls custom-drawn using good old (native) GC and events.
The goal is to implement these custom-drawn controls as good as the native ones (production-ready).
